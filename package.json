{
  "name": "oxray",
  "version": "0.1.0",
  "description": "A fast, opinionated Oxlint and Oxfmt scaffolder",
  "keywords": [
    "formatter",
    "lint",
    "oxc",
    "oxfmt",
    "oxlint",
    "scaffolder",
    "typescript"
  ],
  "homepage": "https://github.com/rayhanadev/oxray#readme",
  "bugs": {
    "url": "https://github.com/rayhanadev/oxray/issues"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rayhanadev/oxray.git"
  },
  "bin": {
    "oxray": "./dist/cli.js"
  },
  "files": [
    "dist/cli.js",
    "dist/plugin.js",
    "LICENSE",
    "README.md"
  ],
  "type": "module",
  "main": "./dist/plugin.js",
  "exports": {
    ".": "./dist/plugin.js",
    "./plugin": "./dist/plugin.js"
  },
  "scripts": {
    "build": "bun run build:cli && bun run build:plugin",
    "build:cli": "bun build src/cli.ts --outfile dist/cli.js --target node --format esm --packages external",
    "build:plugin": "bun build src/plugin.ts --outfile dist/plugin.js --target node --format esm --packages external",
    "format": "oxfmt",
    "lint": "oxlint",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "bun run build && bun test && bun run typecheck"
  },
  "dependencies": {
    "@clack/prompts": "^1.7.0",
    "jsonc-parser": "^3.3.1",
    "nypm": "^0.6.9"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "oxclippy": "^0.2.0",
    "oxfmt": "^0.62.0",
    "oxlint": "^1.77.0",
    "oxlint-tsgolint": "^7.0.2001",
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  }
}
